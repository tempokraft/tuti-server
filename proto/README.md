# proto/

Reference copies only — do not edit these files here.

The source of truth lives in the sibling repo:

```
../tuti/proto/
```

To refresh the copies run:

```bash
make build
# or
./scripts/copy_proto.sh
```
